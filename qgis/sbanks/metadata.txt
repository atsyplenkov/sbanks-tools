# This file contains metadata for your plugin.

# This file should be included when you package your plugin.
# Mandatory items:

[general]
name=sbanks
qgisMinimumVersion=3.0
description=Smooth vector geometries using Savitzky-Golay or Whittaker-Eilers filters with optional spline resampling
version=0.1
author=Anatoly Tsyplenkov
email=hard.sign9115@fastmail.com

about=The plugin performs vector geometry smoothing using Savitzky-Golay or Whittaker-Eilers filters with optional spline resampling. The Whittaker-Eilers filter supports distance-aware smoothing for unevenly spaced data. Uses the sbanks-core library for pure Python smoothing algorithms without additional dependencies.

tracker=https://github.com/atsyplenkov/sbanks-tools/issues
repository=https://github.com/atsyplenkov/sbanks-tools
# End of mandatory metadata

# Recommended items:

hasProcessingProvider=yes
# Uncomment the following line and add your changelog:
# changelog=

# Tags are comma separated with spaces allowed
tags=geometry, polyline, polygon, vector, smoothing, savitzky-golay, whittaker-eilers

homepage=https://github.com/atsyplenkov/sbanks-tools
category=Analysis
icon=icon.svg
# experimental flag
experimental=False

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False

# Since QGIS 3.8, a comma separated list of plugins to be installed
# (or upgraded) can be specified.
# Check the documentation for more information.
# plugin_dependencies=

Category of the plugin: Raster, Vector, Database or Web
# category=Vector

# If the plugin can run on QGIS Server.
server=False
